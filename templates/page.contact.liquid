{{ 'page-forms.css' | asset_url | stylesheet_tag }}
{{ 'page-content.css' | asset_url | stylesheet_tag }}
<h1>{{ page.title }}</h1>
<div class='page-content'>{{ page.content }}</div>
<h1>Enquires</h1>
<div class='page-form'>
{% form 'contact' %}
  {% if form.posted_successfully? %} <p class='success'>Thank you contacting us!<p> {% endif %}
  {{ form.errors | default_errors }}
  <label for="ContactFormName">name</label>
  <input type="text" id="ContactFormName" name="contact[name]">
  <label for="ContactFormEmail">email</label>
  <input type="email" id="ContactFormEmail" name="contact[email]">
  <label for="ContactFormPhone">phone</label>
  <input type="tel" id="ContactFormPhone" name="contact[phone]">
  <label for="ContactFormMessage">message</label>
  <textarea rows="10" id="ContactFormMessage" name="contact[body]"></textarea>
  <input type="submit" value="send">
{% endform %}
</div>

{% style %}
  .contact-details > p {
    padding: 8px;
    font-size: 1.4rem;
  }
  .contact-details > p::first {
    padding-top: 0;
    font-size: 1.4rem;
  }
  .contact-details .contact-us {
    margin: 8px 0;
  }
  .contact-details .contact-us .info {
    width: 100%;
    justify-content: start;
  }
  .page-form {
    max-width: 100%;
    box-sizing: border-box;
  }
  .page-form form {
    padding: 0 16px;
    margin-bottom: 16px;
  }
{% endstyle %}