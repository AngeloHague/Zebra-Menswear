<section id="shopify-section-[id]" class="shopify-section">
    <div class='promo_slider'>
        {% for block in section.blocks %}
            {% case block.type %}
                {% when 'slide' %}
                    <div class='slide'>
                        <div class="bg_blur">
                            <picture>
                                <source media="{max-width:450px}" srcset="{{ block.settings.image_small | image_url }}">
                                        <img src="{{ block.settings.image_small | image_url }}" width='100%' height='100%'>
                            </picture>
                        </div>
                        <a href="{{ block.settings.collection.url }}">
                            <div class="content">
                                <div class="card">
                                    <div class='image'>
                                        <picture>
                                            <source media="{max-width:450px}" srcset="{{ block.settings.image_small | image_url }}">
                                            <img src="{{ block.settings.image_small | image_url }}" width='100%' height='100%'>
                                        </picture>
                                    </div>
                                    <div class='caption'>
                                        <p>{{ block.settings.caption }}</p>
                                    </div>
                                </div>
                            </div>
                        </a>
                    </div>
            {% endcase %}
        {% endfor %}
    </div>
</section>
    

{% schema %}
    {
        "name": "Promo Slider #1",
        "settings": [
        ],
        "max_blocks": 5,
        "blocks": [
            {
                "name": "Slide",
                "type": "slide",
                "settings": [
                    {
                        "type" : "checkbox",
                        "id" : "show_slide",
                        "label" : "Show Slide",
                        "default": false
                    },
                    {
                        "type" : "text",
                        "id" : "caption",
                        "label" : "Slide Caption",
                        "default" : "Explore the collection"
                    },
                    {
                        "type" : "image_picker",
                        "id" : "image_small",
                        "label" : "Slide Image Picker (Small)"
                    },
                    {
                        "type" : "image_picker",
                        "id" : "image_big",
                        "label" : "Slide Image Picker (Large)"
                    },
                    {
                        "type" : "collection",
                        "id" : "collection",
                        "label" : "Collection Link"
                    }
                ]
            }
        ]
    }
{% endschema %}